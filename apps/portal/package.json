{
  "name": "@cksheuen/portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1422",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4422"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
